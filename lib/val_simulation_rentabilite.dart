import 'package:flutter/material.dart';

class ValidationSimulationRentabilite extends StatelessWidget {
  const ValidationSimulationRentabilite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color.fromARGB(25, 25, 255, 255),
        title: const Text("beta")
      )
    );
  }
}